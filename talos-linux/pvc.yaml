apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minecraft-data
  namespace: games
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: usb-storage
  resources:
    requests:
      storage: 50Gi
